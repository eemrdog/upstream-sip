
Installation of "{{ .Chart.Name }}" is completed, release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

To delete release:

    helm uninstall {{ .Release.Name }} --namespace {{ .Release.Namespace }}

Note that ExternalCertificate CustomResourceDefinition resources are not removed when deleting release.

Following ExternalCertificate API versions have been installed:

    com.ericsson.sec.certm/v1alpha2
    certm.sec.ericsson.com/v1beta1
    certm.sec.ericsson.com/v1

Run the following command to list available ExternalCertificate API versions:

    kubectl api-versions | grep certm

Details of ExternalCertificate CustomResourceDefinition resources:

    kubectl get customresourcedefinitions.apiextensions.k8s.io externalcertificates.certm.sec.ericsson.com -o yaml
    kubectl get customresourcedefinitions.apiextensions.k8s.io externalcertificates.com.ericsson.sec.certm -o yaml