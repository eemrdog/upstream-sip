apiVersion: v1
kind: Secret
metadata:
  name: iam-eocecm-client-secret
  labels:
    app: iam-eocecm-client-secret
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-40"
    "helm.sh/hook-delete-policy": before-hook-creation
type: Opaque
stringData: #data:
  eocecm-admin-cli-clientsecret: $(head -c 50 /dev/random | tr -dc 'a-zA-Z0-9' | base64) # {{ randAlphaNum 24 | b64enc | quote }}
